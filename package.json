{
  "name": "nexus-monorepo",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "npm run dev --prefix ui/portal",
    "build": "npm run build --prefix ui/portal",
    "start": "npm run start --prefix ui/portal",
    "vercel-build": "cd ui/portal && rm -rf node_modules package-lock.json && npm install && npm run build"
  },
  "dependencies": {
    "next": "^15.0.3"
  }
}
